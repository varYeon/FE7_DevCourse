{
  // 연습문제

  // 1.
  const str: string = "Hello";
  console.log(str);

  // 2.
  const num: number = 42;
  console.log(num);

  // 3.
  const bool: boolean = true;
  console.log(bool);

  // 4.
  const arr: number[] = [1, 2, 3];
  console.log(arr);

  // 5.
  const arr2: string[] = ["a", "b", "c"];
  console.log(arr2);

  // 6.
  const tuple: [string, number] = ["bsy", 25];
  console.log(tuple);

  // 7.
  const nul: null = null;
  console.log(nul);

  // 8.
  const undi: undefined = undefined;
  console.log(undi);

  // 9.
  const obj: {
    name: string;
    age: number;
  } = {
    name: "sy",
    age: 25,
  };
  console.log(obj);
}
