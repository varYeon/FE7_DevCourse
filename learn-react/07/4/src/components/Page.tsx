import Count from "./Count";

export default function Page() {
  return (
    <>
      <Count
      // count={count}
      // increment={increment}
      // decrement={decrement}
      // reset={reset}
      />
    </>
  );
}
